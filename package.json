{
  "name": "miv-hub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Resource hub for the USTHB Master Informatique Visuelle",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client && npm run seed --prefix server",
    "dev:api": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server",
    "reset": "npm run reset --prefix server"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.6.1",
    "archiver": "^8.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0"
  }
}
